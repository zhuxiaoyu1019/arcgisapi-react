import { CalciteTooltip, CalciteTooltipManager } from "../index";
import { register } from "../utils";
register({
    "calcite-tooltip": CalciteTooltip, "calcite-tooltip-manager": CalciteTooltipManager
});
