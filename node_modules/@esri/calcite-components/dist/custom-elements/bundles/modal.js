import { CalciteModal, CalciteScrim, CalciteIcon, CalciteLoader } from "../index";
import { register } from "../utils";
register({
    "calcite-modal": CalciteModal, "calcite-scrim": CalciteScrim, "calcite-icon": CalciteIcon, "calcite-loader": CalciteLoader
});
