import { CalciteCard, CalciteLoader, CalciteCheckbox } from "../index";
import { register } from "../utils";
register({
    "calcite-card": CalciteCard, "calcite-loader": CalciteLoader, "calcite-checkbox": CalciteCheckbox
});
