import { CalciteInput, CalciteProgress, CalciteIcon } from "../index";
import { register } from "../utils";
register({
    "calcite-input": CalciteInput, "calcite-progress": CalciteProgress, "calcite-icon": CalciteIcon
});
