import { CalciteTile, CalciteIcon, CalciteLink } from "../index";
import { register } from "../utils";
register({
    "calcite-tile": CalciteTile, "calcite-icon": CalciteIcon, "calcite-link": CalciteLink
});
